# Crie um programa que converta a temperatura celsius para fahrenheit
t = float(input('Temperatura em graus celsius: '))
f = (9*t)/5+32
print ('{} graus celcius equivalem a {} fahrenheit.'.format(t, f))